import { MongoClient } from "mongodb"

const  handler = async(req, res) => {
  // if (req.method === 'GET') {
  // const reponse = fetch(`https://jsonplaceholder.typicode.com/users/${userId}/todos`)
  
  // }
}



export default handler